# How many servers to try from discovery
SERVER_LIMIT=12
TRIES_PER_SERVER=2
GLOBAL_MAX_TRIES=10
CMD_TIMEOUT=60
BACKOFF_BASE=5

# Paths (optional override)
BASE_DIR="$HOME/speedtest-logs/data"
SQLITE_DB="$HOME/speedtest-logs/data/telemetry.db"

# Telemetry targets
DNS_TARGET="one.one.one.one"
HTTP_URL="https://www.google.com/generate_204"

